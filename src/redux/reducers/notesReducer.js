const initState = {
  notes: [],
  loading: false,
  error: null,
};

export const notesReducer = (state=initState, action) => {
    switch(action.type){
        default:
            return state
    }
}
